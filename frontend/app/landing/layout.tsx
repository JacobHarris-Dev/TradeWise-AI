/**
 * Landing layout - no auth required, no AppShell
 * Provides a clean landing page experience
 */
export default function LandingLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
